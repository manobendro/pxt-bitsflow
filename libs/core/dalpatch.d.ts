// Supplementary DAL constants needed by the base language runtime that are not in the
// linux variant's committed dal.d.ts. const-enum declarations merge by name.
declare const enum DAL {
    DEVICE_ID_NOTIFY_ONE = 1022,
    DEVICE_ID_NOTIFY = 1023,
    CFG_RAM_BYTES = 205,
}
